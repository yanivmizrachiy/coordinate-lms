# AI agent entry point

This file is a pointer only. It is not a second source of truth.

Before inspecting, editing, cleaning, refactoring, deleting, testing, merging, or deploying this repository, read and obey:

> **`RULES.md`**

Do not copy rules into this file. If a reusable project rule changes, update `RULES.md` only.
