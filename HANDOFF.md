# HANDOFF.md — historical pointer only

`RULES.md` is the **only live source of truth** for this repository.

The previous handoff snapshot was preserved verbatim at:
`_legacy/HANDOFF-2026-07-30.md`.

Do not use the archived handoff for current URLs, deployment state, permissions, attempt policy, page counts or UI requirements. It is historical evidence only. Current behavior and current permissions are defined by `RULES.md`, the implementation and current test/release evidence.
