# HANDOFF — נקודת שמירה בטוחה

**נוצר:** 22.07.2026 · **עודכן:** 30.07.2026 · **ענף:** `main` · **מצב:** ירוק, לא נותרה עבודה שבורה

> **עדכון 30.07.2026 — אחידות מלאה עם פרויקט הזוויות.** סרט הרכבת חזר להיות
> העמוד הראשי (בתוך ה־Hero); כפתורי הפעולה והסרגלים הם עכשיו שפת הזוויות
> אחד־לאחד (`actions.css`: `.btn`/`.wsbar`); נוסף בוחר עמודים (הורדה/הדפסה
> של דפים נבחרים, צבע/שחור־לבן); ו־`#/book` היא חוברת מדפדפת דיגיטלית
> (פורט מלא של FlipbookViewer, `flipbook.ts`+`flipbook.css`+`spreads.ts`),
> כשהגלילה הרציפה להדפסה עברה ל־`#/print`. הפירוט המחייב — בדף הכללים,
> פרקים 8 ו־13.
>
> **המשך 30.07.2026 (ערב):** תוקנו לעומק עמודים 13/21/45 („עמוד על עמוד" —
> שרטוט שברח מהכרטיס; חוק בליעה + בדיקת e2e על כל 74), עמוד 64 (טווח ציר
> לכל שרטוט — נקודה לעולם לא מחוץ למערכת), עמוד 16 (הפוסטר אומר „מילת
> הצופן" בכל ארבעת המופעים — נערך בתמונה עצמה), ועמוד 18 הפך לדף מודפס
> („המשימות שלנו רק להדפסה"). דף הנחיתה: בלי מילות דמו, כותרת הסרטון בנוסח
> של יניב, הלוגו מסתובב, אין קישור „סרט הפתיחה". **שתי כתובות**: הישנה
> (Pages) = החוברת בלבד (`VITE_SITE=booklet`); החדשה (Vercel) = האתר המלא.
> בדיקות: 116 יחידה · 64 Playwright, הכול ירוק.
>
> **המשך 31.07.2026:** הסרט רץ פעם אחת עם קול ונח על תמונה עומדת (בלי
> כפתורים); „התחל" על הכריכה פותח את תוכן העניינים; תוכן עניינים במשבצות
> צבע; מסך תמיד צבעוני והבחירה צבע/שחור-לבן רק בהדפסה; הורדה (PDF מוכן —
> `public/hoveret.pdf`, `npm run pdf`) נפרדת מהדפסה; עמוד 18 בשישה סעיפים;
> ארבעת הפוסטרים בלי שם/כיתה/תאריך ומספרם למעלה-משמאל; דפדוף גם מתחתית
> העמוד; `minimum-scale=1` (תיקן זום-אאוט נדבק במובייל). הכול ירוק ופרוס.
>
> **קישור אחד ויחיד (31.07.2026):** פיצול שתי-הכתובות בוטל — הכתובת המקורית
> (GitHub Pages) היא האתר המלא; Vercel תשתית פריסה בלבד. בסרגל מסגרת
> החוברת: „פתיחת החוברת" במקום „תצוגה מלאה".

---

## מה הושלם בפועל

### 1. אוצר מילים אחיד לתנועה
כל עקומה וכל משך זמן באפליקציה הם עכשיו **טוקן בעל שם** ב־`tokens.css`:
`--ease` · `--ease-pop` · `--ease-sweep` · `--dur-press/quick/move/enter/slow`.
אפס `cubic-bezier` גולמי נשאר ב־`app.css` (היו 13). כיבוי תנועה למי שביקש
(`prefers-reduced-motion`) נעשה **פעם אחת בשורש**, כך שרכיב חדש לא יכול לשכוח.
השהיות (stagger) נשארו מספרים — הן הקצב של רצף מסוים, לא ערך משותף.

### 2. הלוגו הורחק מקצה הסרט
`14px→20px` / `4vw→5vw` / `26px→34px` ב־`app.css`.

**שים לב — יניב תיקן את אותו נושא במקביל** (קומיט `69c13bc`): הוא הוסיף
`src/styles/opening-mobile.css` שמעביר את הלוגו אל הפס הכהה מעל הסרט במסכים
גבוהים, ובדיקה חדשה `tests/e2e/opening-badge.spec.ts`. **שני התיקונים משלימים
ולא מתנגשים** — שלי מרחיק בכל המסכים, שלו מטפל במקרה של מסך גבוה. בוצע rebase
נקי מעל העבודה שלו, והבדיקה שלו **עוברת**.

### 3. „התחל" פותח את השער מיד
`#/menu` → **`#/book`**. הכריכה כבר מפוענחת ברקע בזמן שהסרט רץ, ולכן אין המתנה.
מסך התפריט נשאר קיים וזמין בכתובת `#/menu`.

### 4. תוכן העניינים עוצב מחדש
מחמישה גושי צבע רוויים ל**כרטיסים לבנים** עם פס צבע בקצה המוביל, „פרק N",
שם הפרק, ומספר העמוד בקצה הנגדי. סדר קריאה: **שם → מספר**.
נותן מראה של ספר ולא של תפריט אתר, **ומדפיס בעשירית הדיו**.

### 5. `vercel.json` נוסף
`buildCommand`, `outputDirectory: dist`, rewrites ל־SPA, וכותרות cache.
**הריפו מוכן לחיבור ל־Vercel — החיבור עצמו עדיין לא בוצע** (ראו „מה עדיין חסר").

---

## קבצים שהשתנו

| קובץ | מה |
|---|---|
| `src/styles/tokens.css` | טוקני התנועה + כיבוי בשורש |
| `src/styles/app.css` | אימוץ הטוקנים · הזזת הלוגו |
| `src/styles/workbook.css` | עיצוב מחדש של תוכן העניינים |
| `src/views/home.ts` | „התחל" → `#/book` |
| `src/views/tocSheet.ts` | מבנה השורה: פס · פרק · שם · מספר |
| `tests/layout-rules.test.ts` | בדיקות אוצר המילים של התנועה |
| `tests/e2e/smoke.spec.ts` | עודכנו ליעד החדש של „התחל" ולסדר הקריאה החדש |
| `vercel.json` | **חדש** |

---

## בדיקות

| | תוצאה |
|---|---|
| `npx tsc --noEmit` | ✅ נקי |
| `npx vitest run` | ✅ **105 עברו**, 0 נכשלו |
| Playwright מלא (desktop), **אחרי** ה־rebase | ✅ **34 עברו** |
| `tests/e2e/opening-badge.spec.ts` (הבדיקה של יניב) | ✅ עוברת |
| Playwright — ממוקד (חיתוך · תוכן עניינים · לוגו · „התחל") | ✅ **4 עברו** |
| `npm run verify` מלא (הרצה אחרונה לפני `vercel.json`) | ✅ 105 יחידה + 53 דפדפן |

`vercel.json` הוא קובץ תצורה בלבד ואינו משפיע על אף בדיקה.

---

## מה עדיין חסר

**כלום.** חיבור Vercel בוצע (22.07.2026): הפרויקט מקושר לחשבון
`yanivs-projects-322b2b37`, ריפו GitHub מחובר לפריסה אוטומטית בכל push,
והייצור חי ב־https://coordinate-first-quadrant.vercel.app — לצד GitHub Pages.

## הפעלה

```bash
npm install
npm run dev
```

בדיקות: `npm run verify` (טיפוסים · יחידה · בנייה · דפדפן).

---

## הצעד הבא המדויק

**לחבר את הריפו ל־Vercel** (פעולה של יניב, דקה אחת), ואז לוודא שהכתובת החדשה
מציגה את אותו דבר כמו GitHub Pages.

לאחר מכן, אם רוצים להמשיך בשיפורי הביצועים: המועמד היחיד ששווה בדיקה הוא
**פיצול קוד** — מסך הפתיחה מוריד היום 272kB שכוללים את כל 74 העמודים, אף שהוא
צריך כמעט כלום מהם. זה טרם נמדד לעומק ולא אומת.

---

## מדידות שכבר נאספו — אין צורך למדוד שוב

נמדד על build ייצור, שרת מקומי, Chromium שולחני:

| | |
|---|---|
| חבילה | `index.js` 272.7kB (48.7kB gzip) · `style.css` 38.0kB (9.1kB gzip) |
| פתיחה `#/` | FCP **144ms** · 148 אלמנטים · 0 SVG |
| תפריט `#/menu` | FCP **80ms** · 125 אלמנטים |
| עמוד `#/workbook/30` | FCP **136ms** · 327 אלמנטים · 1 SVG |
| חוברת `#/book` | FCP **488ms** · 12,249 אלמנטים · 97 SVG · 1,867 `<text>` |
| התייצבות החוברת | **972ms** |
| מעבר תפריט→עמוד | **48ms** · מעבר לחוברת אינו חוסם (1ms) |

**המסקנה: האפליקציה כבר מהירה.** אין צוואר בקבוק שמצדיק שכתוב. המועמד היחיד
ששווה בדיקה הוא פיצול קוד — מסך הפתיחה מוריד 272kB שכוללים את כל 74 העמודים אף
שהוא צריך כמעט כלום מהם. **טרם נמדד לעומק ולא אומת.**

---

## אזהרה לסשן הבא

סקר עומק על מהירות/מבנה/נגישות **קרס באמצע** (נגמרה מכסת הסשן; 148 מתוך 155
סוכנים נכשלו, 0 ממצאים שרדו אימות). **אין לפעול לפי תוצריו החלקיים.** אם רוצים
את הסקר — יש להריץ אותו מחדש כשיש מכסה.

---

## כתובת האתר החי

https://yanivmizrachiy.github.io/coordinate-first-quadrant/
