# USER_MEMORY — retired historical archive

This file is **not a source of rules** and must not be used to decide current LMS behavior.

The repository has exactly one normative source of truth:

> `RULES.md`

Older workbook decisions that were previously stored here were removed from the active branch because they contained obsolete page counts, publication instructions, and implementation assumptions that could conflict with the current source of truth.

For current work:
- the printable workbook in `yanivmizrachiy/coordinate-first-quadrant` is read-only;
- every canonical printable page must become a computerized page;
- the printable content/design is preserved as the base;
- digital interaction and pedagogical adaptations exist only in the LMS layer;
- all current requirements are defined only in `RULES.md`.
