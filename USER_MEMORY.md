# USER_MEMORY.md — historical pointer only

`RULES.md` is the **only live source of truth** for this repository.

The previous large workbook decision log was preserved verbatim at:
`_legacy/USER_MEMORY-2026-07-22.md`.

Do **not** add new requirements, operating rules, merge permissions, deployment permissions, UI policy, scoring policy or architecture rules to this file or to the archived copy. Put every current rule in `RULES.md` and keep implementation + tests aligned with it.

The archive is evidence/history only. Any statement in it that conflicts with `RULES.md` is obsolete by definition.
